# https://rust-lang.github.io/mdBook/format/configuration/general.html
[book]
title = "ALS-U EPICS Envorinment Training"
authors = ["Jeong Han Lee"]
src = "docs"
language = "en"
multilingual = false

[build]
build-dir = "public"              # the directory where the output is placed
create-missing = true             # whether or not to create missing pages
use-default-preprocessors = true  # use the default preprocessors
extra-watch-dirs = []             # directories to watch for triggering buildsi

[preprocessor.index]

[preprocessor.links]

[output.html]
default-theme = "light"
preferred-dark-theme = "navy"
smart-punctuation = true
#additional-css = ["custom.css", "custom2.css"]
#additional-js = ["custom.js"]
no-section-label = false
git-repository-url = "https://git.als.lbl.gov/jeonglee/epics-trainings"
git-repository-icon = "fa-gitlab"
#edit-url-template = "https://git.als.lbl.gov/jeonglee/epics-trainings/edit/master/guide/{path}"
#site-url = "/example-book/"
#cname = "myproject.rs"
#input-404 = "not-found.md"
mathjax-support = true

[output.html.print]
enable = true     # include support for printable output
page-break = true # insert page-break after each chapter

[output.html.fold]
enable = false    # whether or not to enable section folding
level = 0         # the depth to start folding

[output.html.search]
enable = true            # enables the search feature
limit-results = 30       # maximum number of search results
teaser-word-count = 30   # number of words used for a search result teaser
use-boolean-and = true   # multiple search terms must all match
boost-title = 2          # ranking boost factor for matches in headers
boost-hierarchy = 1      # ranking boost factor for matches in page names
boost-paragraph = 1      # ranking boost factor for matches in text
expand = true            # partial words will match longer terms
heading-split-level = 3  # link results to heading levels
copy-js = true           # include Javascript code for search
